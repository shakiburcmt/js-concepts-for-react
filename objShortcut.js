const name = 'Shakib';
const wifeName = 'Mariam';
const obj = {
    name:name,
    wifeName:wifeName
}
console.log(obj)
// now time to declare shortcut
const object = { name, wifeName };
// ekhane name dile js automatic dhore ney je name ekta variable o ache
console.log(object)